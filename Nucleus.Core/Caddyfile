nucleus.pluscosmic.dev {
    reverse_proxy nucleus:8080
    
    # Optional: Add logging
    log {
        output file /var/log/caddy/access.log
    }
    
    # Optional: Add compression
    encode gzip
}